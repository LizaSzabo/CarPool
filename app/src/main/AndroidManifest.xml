<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.aut.android.carpool">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".CarPoolApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_car_rental_24"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_car_rental_24"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity
            android:name=".ui.appcontent.ContentActivity"
            android:exported="false"
            android:label="@string/title_activity_content"
            android:theme="@style/Theme.CarPool.NoActionBar">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>